package de.tum.in.tumcampus.test;

import android.test.AndroidTestCase;

public class DatabaseTest extends AndroidTestCase {

	/*
	private Database db;

	protected void setUp() {
		super.setUp();

		db = new Database(this.getContext(), "test.db");
		assertTrue(db.getWritableDatabase().isOpen());
	}

	protected void tearDown() {
		db.close();
		super.tearDown();
	}

	public final void testAddRunType() {
		RunType rt = db.new RunType("Marathon2", 10);

		assertTrue(db.getRunTypes().contains(rt));
		
		System.out.println("blabla " + db.getRunTypes().indexOf(rt));
	}
	*/
}